[package]
name = "generate-dhcp-dns"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
thiserror = "1.0"
lazy_static = "1.4.0"
regex = "1.4.5"
indoc = "1.0"

[profile.release]
lto = true
